# BUG-014: TASK-014 merge conflict in registry.go

**Task:** TASK-014 — Rust and Java Tree-Sitter Grammars
**Author:** bravo-2
**Branch:** task/014
**Rejected by:** AUDIT
**Date:** 2026-03-15

## What Failed

Merge conflict in `internal/parser/registry.go`. Both TASK-013 (merged) and TASK-014 add `NewDefaultRegistry()` to the same file. TASK-013's version has 4 parsers (Go, Python, TS, JS), while TASK-014's has 6 (adds Rust + Java).

## Conflict Location

`internal/parser/registry.go` — `NewDefaultRegistry()` function.

## How to Fix

1. `git pull origin main --rebase`
2. Resolve the conflict in `registry.go`: keep the 4 parsers from main (TASK-013) and add Rust + Java registrations
3. The correct `NewDefaultRegistry()` should register all 6 parsers
4. Run `make test` to verify
5. Push the rebased branch

## Code Review Notes

The code itself is APPROVED — well-structured, follows existing patterns, good test coverage. Only needs the rebase.
