# BUG-002: TASK-002 Missing Required Tests

**Related Task:** TASK-002
**Found by:** audit
**Date:** 2026-03-15 15:05
**Severity:** Blocker

## What Failed

TASK-002 (Configuration System) was submitted for review with **zero test files**. The task specification explicitly requires `internal/config/config_test.go` with 7+ unit tests:

1. Test Default() returns correct default values for all fields
2. Test Load() with JSON file overrides specified fields, keeps defaults for others
3. Test Load() without JSON file returns all defaults
4. Test Validate() accepts valid config
5. Test Validate() rejects invalid transport ("invalid", "")
6. Test Validate() rejects invalid MaxFileSize (-1)
7. Test RepoHash() same path → same hash, different paths → different hashes
8. Test Environment variable ENGRAM_DATABASE_PATH overrides default

**None of these tests exist.** The branch contains only `config.go` and `defaults.go`.

## Code Quality Notes

The implementation code itself is reasonable:
- Config struct matches spec
- Load() correctly implements precedence (env > JSON > defaults)
- Validate() checks transport and MaxFileSize
- RepoHash() and DatabaseDir() work correctly
- applyRawOverrides() silently ignores json.Unmarshal errors (minor, fix when adding tests)

## How to Fix

1. Create `internal/config/config_test.go`
2. Implement all 8 tests listed above
3. Run `go test ./internal/config/ -v` and verify all pass
4. Push to branch `task/002` and move task back to review
