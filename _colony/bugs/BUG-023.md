# BUG-023: TASK-023 merge conflict in go.mod

**Task:** TASK-023 — Incremental Re-Indexing with --watch Mode
**Branch:** task/023
**Rejected by:** AUDIT
**Date:** 2026-03-15

## What Failed

Merge conflict in `go.mod`. TASK-033 (bubbletea TUI) was merged and added dependencies to go.mod. TASK-023 also adds fsnotify, causing a conflict.

## How to Fix

1. `git pull origin main --rebase`
2. Resolve go.mod conflict (keep all from main + add fsnotify)
3. Run `go mod tidy && make test`
4. Push the rebased branch
